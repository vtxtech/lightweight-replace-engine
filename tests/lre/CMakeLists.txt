SET(TARGET_SRC
    unitTest_main.cpp
    LRETest.cpp
)

SET(TARGET_COMMON_LIBRARIES
    ${GTEST_LIBRARY}
    ${GTEST_MAIN_LIBARY}
    LRE
)

SET(TARGET_LIBRARIES_VARS ${TARGET_LIBRARIES_VARS}
#none
)

SETUP_TEST(lre)
